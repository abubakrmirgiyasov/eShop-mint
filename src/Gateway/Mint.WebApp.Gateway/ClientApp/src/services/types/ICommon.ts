export interface ILanguage {
  name: string;
}

export interface ITheme {
  name: string;
}

export enum Colors {
  primary,
  light,
  success,
  dark,
  info,
  warning,
  danger,
}

export interface ISampleType {
  value: string;
  label: string;
}
