using Mint.Infrastructure.Repositories.Admin.Interfaces;
using Mint.WebApp.Admin.DTO_s.Categories;
using Moq;

namespace Mint.WebApp.Test;

public class CategoriesTest
{
    [Fact]
    public void Test1()
    {
        // Arrange
        //var mock = new Mock<ICategoryRepository>();
        //mock.Setup(repo => repo.GetCategoriesAsync()).Returns(new Task<IEnumerable<CategoryFullViewModel>>());
   
        //var controller = new 
    }
}